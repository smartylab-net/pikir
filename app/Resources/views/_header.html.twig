{% block header %}
        <div class="row clearfix">
            <div class="col-md-12 column">
                <div class="navbar navbar-inverse">
                    <div class="container">
                        <div class="navbar-header">
                            <a class="navbar-brand" href="{{ path('info_complaint_homepage') }}">Книга жалоб и предложений<span class="badge">beta</span></a>
                        </div>
                        <div class="navbar-collapse collapse navbar-inverse-collapse">
                            {{ knp_menu_render('main') }}
                            <form class="navbar-form navbar-left">
                                <input id="searching" class="form-control col-lg-8" placeholder="Search" type="text">
                            </form>
                            <ul class="nav navbar-nav navbar-right">
                                {% if not app.user %}
                                <li><a href="{{ path('sonata_user_security_login') }}">Войти</a></li>
                                {% else %}
                                <li><a href="{{ path('sonata_user_profile_show') }}">Профиль</a></li>
                                <li><a href="{{ path('sonata_user_security_logout') }}">Выйти</a></li>
                                {% endif %}
                            </ul>
                        </div>

                    </div>
                </div>
            </div>
        </div>
{% endblock %}