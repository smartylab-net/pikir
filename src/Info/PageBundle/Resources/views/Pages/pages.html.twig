{% extends  '::base.html.twig' %}

{% block title %}
    {{ parent() }} : {{ page.title }}
{% endblock %}
{% block body %}

    <div class="page-header">
        <h1>{{ page.title }}</h1>
    </div>

    <article class="blog-article">
        {{ page.content|raw }}
    </article>
{% endblock %}
